# Backlog

This is the stable backlog entrypoint for `xian-linter`.

Current themes:

- keeping the package aligned with `xian-contracting`
- improving packaging and service ergonomics without adding rule duplication

