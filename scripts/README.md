# Scripts

- `release_context.py` validates the checked-in release manifest, resolves a
  tag to an immutable source commit, checks pinned sibling source and lockfile
  versions, and verifies Python distribution metadata.
